# Server
PORT=3001
JWT_SECRET=change-this-to-a-long-random-secret

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=doctor
DB_PASSWORD=change-this-password
DB_NAME=doctor

# SMTP (email verification & password reset)
# Leave empty to log codes to console (dev mode)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@dr-benali.dz
