# Server
PORT=3001
JWT_SECRET=change-this-to-a-long-random-secret

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=doctor
DB_PASSWORD=change-this-password
DB_NAME=doctor

# Email (verification & password reset)
# Option 1: Brevo HTTP API (recommended - easy setup, 300 emails/day free)
# 1. Sign up at https://www.brevo.com (free: 300 emails/day)
# 2. Go to Senders & Domains > Senders > Add a Sender (verify your email)
# 3. Go to SMTP & API > API Keys > Create a new API key (v3)
BREVO_API_KEY=
BREVO_FROM=oh_djaidja@esi.dz
BREVO_FROM_NAME=Cabinet Dr. Djaidja
# Option 2: Resend HTTP API (needs domain verification for sending to anyone)
RESEND_API_KEY=
RESEND_FROM=onboarding@resend.dev
# Option 3: SMTP (blocked on Render Free Tier)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@dr-djaidja.dz
